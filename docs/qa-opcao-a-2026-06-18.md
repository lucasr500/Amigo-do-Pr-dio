# QA — Coerência da opção A por persona (2026-06-18)

**Método:** auditoria de código (não foi possível logar interativamente como cada papel).
**Achado-chave de partida:** as personas de login reais são **só `manager` e `resident`**
(`lib/profile-mode.ts:1`). **Conselho e Funcionário NÃO existem como persona de login** — exatamente
a indefinição prevista. Council existe só como *view-mode de simulação* (o síndico pré-visualiza);
Funcionário não existe em lugar nenhum (nem em `CommunityRole`, que é manager|council|resident|viewer).

**Correção feita nesta rodada (leak real):** `CommunidadeTab` defaultava o papel para "manager",
então o morador via a camada de gestão por default. Travado por `lib/community-view.resolveCommunityRole`
(morador → "resident", sem seletor de simulação). Commit `9214c85` + 3 testes.

---

## A. Transversal
| Item | Veredito | Evidência |
|---|---|---|
| Barra 5 itens opção A | 🟡 **GO (síndico) / FLAG (morador)** | Síndico: Início·Memória·[Perguntar]·Comunidade·Ajustes (`BottomNav.tsx:111-118`). **Morador tem barra diferente:** Início·Comunidade·[Canal]·Agenda·Info (`BottomNav.tsx:121-128`) — opção A foi decidida p/ o síndico; layout do morador não foi redefinido |
| "Mais" não existe | ✅ GO | `CondominioTab` deletada; sem item "Mais" nem atalho |
| Sem tela em branco/404 | 🟡 GO / latente | tsc prova zero link morto; **latente:** `memoria` só renderiza p/ manager (`page.tsx:379`) — morador não tem rota p/ lá, mas se tivesse, blank. Sem caminho real hoje |
| Copy nova | ✅ GO | "Ajustes", "Comunidade", "Guia de situações" aplicados |
| [Perguntar] inerte / IA off | 🟡 **GO (IA) / FLAG (inerte)** | É o AssistantTab (KB determinístico): **não chama IA externa nem rede** (`ai_layer_enabled:false`) ✅; **mas gera respostas da base curada** — NÃO é um slot "em breve". O checklist esperava inerte |
| Nenhuma flag de exposição ligada | ✅ GO | todas false (`feature-flags.ts`) |

## B.1 Morador (prioritária)
| Aba | Veredito | 🚫 Não pode aparecer |
|---|---|---|
| Início | ✅ GO | `ResidentHomeTab` (resident-only); sem tarefas de gestão |
| Memória | ✅ GO (não-leak) / 🟡 FLAG | **morador não tem aba Memória** → sem leak; documentos/timeline do morador vivem em **Comunidade** (filtrados). Placement difere do checklist |
| Comunidade → Transparência | ✅ GO | só agregado (`transparency.ts` exclui inadimplência/unidade); revisão de gestão gated `!isResidentView` (`CommunidadeTab`) |
| Comunidade → Comunicação | ✅ GO | morador travado em "resident" (fix `9214c85`) → conteúdo filtrado |
| Ajustes | ✅ GO (não-leak) / 🔴 **FLAG UX** | **morador não tem aba Ajustes** → Funcionários/Implantação inacessíveis ✅; **mas morador também não tem acesso a conta/perfil/notificações/backup** — lacuna de UX (não é leak) |

## B.2 Síndico
| Aba | Veredito | Evidência |
|---|---|---|
| Início | ✅ GO | `HomeTab` cockpit do síndico |
| Memória | ✅ GO | `MemoriaTab` (manager) |
| Transparência | ✅ GO | agregado + Revisão detalhada (camada de gestão, `!isResidentView`) |
| Ajustes | ✅ GO | manager vê Funcionários + Implantação (`AjustesTab` gated por `manager`); cruzado com B.1 → morador não alcança |
| Guia de situações | ✅ GO | renomeado; em Ferramentas (acessível por Ajustes→Ferramentas) |

## B.3 Conselho · B.4 Funcionário
🔴 **NÃO TESTÁVEL — persona inexistente como login.** `ActiveProfile` = manager|resident apenas.
- **Conselho:** só como view-mode simulado pelo síndico. Limite "conselho vê vs. síndico (lê vs. edita)"
  **não está cravado** → decisão do Lucas.
- **Funcionário:** inexistente (nem em `CommunityRole`). É Completo-Pleno (não bloqueia exposição) →
  decisão do Lucas se/quando implementar.

## C. Deep-links
| Origem → Destino | Veredito |
|---|---|
| memoria-institucional → Memória | ✅ GO (`page.tsx handleNavigateToSection`) |
| central-digital → Comunidade | ✅ GO |
| dados → Ajustes | ✅ GO |
| onOpenMonthlyReview → Transparência (Comunidade, gestão) | ✅ GO; morador só vê agregado (revisão gated) |
| documentos → Memória · operacao/implantacao → Ajustes | ✅ GO |
| Demais (7+) | ✅ GO — **tsc prova zero link morto** (removido "condominio" de AppTab) |

## D. Testes automatizados
- **Leak por papel na camada de dados:** já coberto e verde — `documents.test` (morador só vê
  moradores/público), `timeline-unified.test`, `transparency.test` (sem inadimplência),
  `resident-home.test`, `predio-context.test` (morador sem decisões), e agora `community-view.test`
  (morador travado). **São as travas de regressão de leak.** ✅
- **Smoke tests de RENDER** (barra renderiza 5 itens; AjustesTab esconde Funcionários p/ morador;
  [Perguntar] não dispara IA): exigem **jsdom + @testing-library** (o vitest atual é `environment:node`,
  sem DOM). Adicionar = **novas deps** → 🟡 **decisão do Lucas** (invariante: sem deps sem justificativa).
  A lógica equivalente já está testada via libs puras.

## E. Aceite
**Veredito: GO no critério de leak por papel** — o único vazamento real (Comunidade default manager)
foi **corrigido e travado por teste**. Nenhum outro leak de gestão→morador encontrado.

**Itens que precisam de DECISÃO do Lucas (não improvisar):**
1. **Morador sem superfície de Ajustes** (conta/perfil/notificações/backup) — adicionar Ajustes à
   barra do morador? (a barra do morador não foi redefinida como opção A).
2. **[Perguntar]**: manter o assistente de KB funcional (atual) ou torná-lo um slot "em breve"
   inerte até o RAG? Hoje responde da base curada (sem IA externa).
3. **Conselho/Funcionário**: limites de papel indefinidos; personas de login inexistentes.

**Itens "não testável agora" → tarefa de teste (seção D):** smoke tests de render (precisam de jsdom).
